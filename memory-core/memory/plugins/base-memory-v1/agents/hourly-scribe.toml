id = "hourly-scribe"
version = "0.1.0"
harness = "claude-code"
provider = "claude"
model = "opus"
tools = []
prompt_paths = ["prompts/hourly-scribe.md"]
reference_paths = ["prompts/hourly-scribe-task.md"]
experience = []

[memory]
mode = "runtime_lived_experience"
attach = "new"
