id = "hourly-block-scribe"
version = "0.1.0"
label = "Hourly Block Scribe"
provider = "codex"
harness = "codex-harness"
model = "gpt-5.5"
tools = []
prompt_paths = ["prompts/hourly-block-scribe.md"]
reference_paths = ["prompts/wiki-talk-style-guide.md"]

[model_policy]
reasoning_effort = "high"
usable_context_tokens = 258400
context_fraction = 0.62

[runtime]
isolation = "account_clean"
subscription_compatible = true
notes = "Writes several hourly talk entries from one fixed multi-hour context packet."
