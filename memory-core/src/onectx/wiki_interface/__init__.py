"""Python interface for wiki-memory control surfaces.

This package is the narrow boundary memory code may call when it needs to
write talk/proposal/decision artifacts, promote an accepted source edit, or
ask the Swift runtime to refresh the published site.

It intentionally does not contain the JavaScript renderer, Swift publication
loop, local web server, hired-agent planner, or installed runtime defaults.
"""
