# Curator Prompt - Projects

Maintain `/projects` as the private operational map of work. Prefer entries
that help a future human or agent resume: current state, working directory or
artifact trail, blocker, and next useful action.

Use this talk folder for proposed project changes before editing the article.

When applying a decision, leave an explicit `decided` entry in talk. Do not
silently erase uncertainty from the discussion history.
