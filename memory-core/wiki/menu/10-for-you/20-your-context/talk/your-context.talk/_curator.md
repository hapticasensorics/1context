# Curator Prompt - Your Context

Maintain `/your-context` as durable private guidance for future collaborators.
The page should read as a calm orientation document, not a transcript digest.

Before editing the article, use this talk folder to test whether the observation
is stable, useful, and scoped to the right section.

When applying a decision, leave an explicit `decided` entry in talk. Do not
silently erase uncertainty from the discussion history.
