# Curator Prompt - Topics

Maintain `/topics` as the private concept index. Prefer concise entries that
make repeated ideas findable without turning every passing phrase into a page.

Use this talk folder for proposed concepts, category changes, merges, splits,
and evidence notes before editing the article.

When applying a decision, leave an explicit `decided` entry in talk. Do not
silently erase uncertainty from the discussion history.
